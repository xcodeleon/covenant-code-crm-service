#name: Deploy to Production
#
#on:
#  push:
#    branches: [main]
#
#concurrency:
#  group: production-deploy
#  cancel-in-progress: false
#
#env:
#  IMAGE_NAME: crm-service
#  STACK_NAME: crm
#  SERVICE_NAME: crm_crm-service
#
#jobs:
#  build-and-push:
#    name: Build & Push Image
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Set up Java 21
#        uses: actions/setup-java@v4
#        with:
#          java-version: '21'
#          distribution: 'temurin'
#          cache: maven
#
#      - name: Build JAR
#        run: mvn package -DskipTests -q
#
#      - name: Set up Docker Buildx
#        uses: docker/setup-buildx-action@v3
#
#      - name: Log in to registry
#        uses: docker/login-action@v3
#        with:
#          registry: ${{ secrets.REGISTRY }}
#          username: ${{ secrets.REGISTRY_USER }}
#          password: ${{ secrets.REGISTRY_PASSWORD }}
#
#      - name: Build and push Docker image
#        uses: docker/build-push-action@v5
#        with:
#          context: .
#          push: true
#          tags: |
#            ${{ secrets.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
#            ${{ secrets.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
#          cache-from: type=gha
#          cache-to: type=gha,mode=max
#
#  deploy:
#    name: Deploy & Verify
#    runs-on: ubuntu-latest
#    needs: build-and-push
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Copy stack file to server
#        uses: appleboy/scp-action@v0.1.7
#        with:
#          host: ${{ secrets.SSH_HOST }}
#          username: ${{ secrets.SSH_USER }}
#          key: ${{ secrets.SSH_PRIVATE_KEY }}
#          source: docker-stack.yml
#          target: /opt/crm/
#
#      - name: Deploy and verify rollout
#        uses: appleboy/ssh-action@v1.0.3
#        with:
#          host: ${{ secrets.SSH_HOST }}
#          username: ${{ secrets.SSH_USER }}
#          key: ${{ secrets.SSH_PRIVATE_KEY }}
#          script: |
#            set -euo pipefail
#
#            REGISTRY="${{ secrets.REGISTRY }}"
#            IMAGE_TAG="${{ github.sha }}"
#            SERVICE="${{ env.SERVICE_NAME }}"
#            STACK="${{ env.STACK_NAME }}"
#            NEW_IMAGE="$REGISTRY/${{ env.IMAGE_NAME }}:$IMAGE_TAG"
#
#            export REGISTRY IMAGE_TAG
#            export DB_NAME="${{ secrets.DB_NAME }}"
#            export DB_USER="${{ secrets.DB_USER }}"
#
#            echo "==> Deploying image: $NEW_IMAGE"
#            docker stack deploy -c /opt/crm/docker-stack.yml "$STACK" --with-registry-auth
#
#            echo "==> Monitoring rollout (max 5 min)..."
#            MAX_WAIT=300
#            INTERVAL=15
#            ELAPSED=0
#
#            while [ "$ELAPSED" -lt "$MAX_WAIT" ]; do
#              REPLICAS=$(docker service ls --filter "name=$SERVICE" --format "{{.Replicas}}" 2>/dev/null || echo "0/0")
#              RUNNING=$(echo "$REPLICAS" | cut -d'/' -f1)
#              DESIRED=$(echo "$REPLICAS" | cut -d'/' -f2)
#
#              FAILED=$(docker service ps "$SERVICE" --no-trunc \
#                --format "{{.Image}} {{.CurrentState}}" 2>/dev/null \
#                | grep "$IMAGE_TAG" | grep -c "Failed" || echo 0)
#
#              echo "  [${ELAPSED}s] Replicas: $REPLICAS | Failed new tasks: $FAILED"
#
#              if [ "$FAILED" -gt 0 ]; then
#                echo "==> ERROR: new image failed health check — rolling back"
#                docker service rollback "$SERVICE" || true
#                exit 1
#              fi
#
#              if [ "$DESIRED" -gt 0 ] && [ "$RUNNING" = "$DESIRED" ]; then
#                SPEC_IMAGE=$(docker service inspect "$SERVICE" \
#                  --format '{{.Spec.TaskTemplate.ContainerSpec.Image}}' 2>/dev/null || echo "")
#                if echo "$SPEC_IMAGE" | grep -q "$IMAGE_TAG"; then
#                  echo "==> Deployment successful: all $DESIRED replica(s) running"
#                  exit 0
#                fi
#              fi
#
#              sleep "$INTERVAL"
#              ELAPSED=$((ELAPSED + INTERVAL))
#            done
#
#            echo "==> ERROR: timeout after ${MAX_WAIT}s — rolling back"
#            docker service rollback "$SERVICE" || true
#            exit 1
